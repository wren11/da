#pragma once
#include "pch.h"

namespace DarkAges {
    namespace Operations {
        struct GameConstants {
            static constexpr DWORD INTERVAL_WALK_MS = 750;
            static constexpr DWORD INTERVAL_SEND_MS = 250;
            static constexpr DWORD THREAD_TIMEOUT_MS = 2000;
        };
    }
}

